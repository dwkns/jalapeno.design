<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{% block title %}{{ siteName }}{% if title %} &raquo; {{ title }}{% endif %}{% endblock %}</title>
    {% include "seo.njk" %}
    <!-- styles -->
    <link rel="stylesheet" href="/assets/css/tailwind.css?v={% version %}"/>
    <!-- favicon -->
    <link rel="icon" href="/assets/images/logo.svg" type="image/svg+xml"/>
    {% if slideshow %}<!-- Slideshow: https://pagedone.io/docs/carousel-->
    <link href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" rel="stylesheet"/>{% endif %}
    {% block head %}{% endblock %}
  </head>

  <body class="bg-white dark:bg-gray-900 text-black dark:text-white">
    {% include "tw_size.njk" %}
    <div class="md:flex w-full min-h-screen mx-auto">

      <!-- column sidebar -->
      <div class="flex-auto basis-1/4">
        {% include "sidebar-l.njk" %}
        
      </div>

      <!-- column mainbar -->
      <div class="flex-auto basis-1/2">
      <div class="w-[20px] bg-[url(/assets/images/optim.svg)] eleventy:ignore h-full fixed dark:bg-[url(/assets/images/optim_dark.svg)]"></div>       
        <main role="main" class="w-full pl-[20px]">
    {% block content %}
      {{ content | safe }}
    {% endblock %}
        </main>
      </div>

      <!-- column sidebar -->
      <div class="flex-auto basis-1/4">
        {% include "sidebar-r.njk" %}
      </div>

    </div>
  </body>

</html>